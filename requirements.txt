transformers>=4.36.0
peft>=0.7.0
accelerate>=0.25.0
torch>=2.0.0
gradio>=4.0.0
huggingface-hub>=0.19.0
sentencepiece>=0.1.99
protobuf>=3.20.0
safetensors>=0.4.0

# Coqui TTS - For distinct character voices
TTS>=0.22.0

# Fallback to gTTS if Coqui fails
gTTS>=2.3.0

# Optional: For voice speed adjustment
pydub>=0.25.1

# Audio processing
soundfile>=0.12.1
librosa>=0.10.0